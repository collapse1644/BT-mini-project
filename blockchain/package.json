{
  "name": "speedrun-verification-blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "node": "hardhat node --hostname 127.0.0.1",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17"
  }
}
